# v16 操作台补充独立审核

范围：独立底座和轮盘素材、24 帧操作员的接触关系，以及控制/受惊/逃岗/暂停/重置切换。没有修改游戏代码，没有开展全版玩家评分，也没有操作共享浏览器。

## 审核结论：已闭环

- **已关闭 P2：轮盘旋转符号与人物手势相反。** 修复后的 `desiredAngle=(5-target)*.14` 与素材手势方向一致。复核独立运行得到 `{steering:-1,frame:0,angle:+0.7}` 和 `{steering:1,frame:10,angle:-0.7}`；方向测试期望同步修正。没有剩余阻塞项。

## 已核查

- 底座独立绘制，固定目标矩形 `(411.6,653.3,76.8,102.4)`；轮盘仅围绕 `(450,673)` 旋转，当前轮缘范围与之前校准的人物手套范围基本一致。
- 受惊分支使用人物单帧，不会再混入旧的整台机器；受惊及逃岗保留当前轮盘角度。
- 暂停不推进世界时间；重载关卡调用 `operatorAnimation.reset()`；轮盘角度和人物状态一起归零。
- 查看了 `console-layers-v16.png`、对应完整 prompt 以及 `output/playwright/v16-wheel-desktop.png`。
- 修复后再次独立运行 `node --test sand-marbles/tests/operator-animation.test.cjs`：5/5 通过；另以双向端点脚本确认人物帧和轮盘角度的对应关系。

浏览器双向旋转、暂停和底座固定结果由主 Agent 提供，本审核没有独立重跑其浏览器脚本。
