# 国王消消乐 v3 验证记录

## 本地规则与物理

2026-09-17，`node --test king-match/tests/*.test.cjs sand-marbles/tests/*.test.cjs`：**51/51 通过**，包含国王游戏 24 项及另一游戏 27 项回归。

- 起盘 432 个真实颗粒，ready 不推进模拟、不扣血。真实顶部空位和可达容量约束补料，画外待入库存不施力。
- 两关零输入：约 37.4 秒第一次触刺，HP 100→88，仍 playing；持续触刺约 6.4 秒后第九次伤害归零才 lost。边界、保护腔及完整棋盘无泄漏，已入颗粒满足 `spawned = active + collected`。
- 从模拟 8 秒开始按真实交换状态机求解：第一关约 20.6 秒、第二关约 20.6 秒获救；清路后连续 5 秒主仓填充指标高于 0.85，同时真实排石超过 80 颗。最终主仓指标约 0.98，压力下降后国王回推。
- 先完成部分三消、等真实首伤后再完成最后一次交换：第一关剩余 40 HP、第二关剩余 16 HP 获救。两关实际离刺后至少 2 秒不再受伤，不恢复已损 HP。
- 移动宝石碰撞体与可见 y 共用数据；测试旧槽已空、不留下幽灵碰撞，移动砖内无法留存穿透颗粒。列内顺序、石台分段支撑、消除数守恒和连锁终止均通过。
- 两关不能分别通过两步/三步以内的正常交换直接打开出口；起盘无三连、四种颜色，后续求解包含重力与全部连锁。

额外使用两种粒子随机种子（721、1907），两关共四次解法仿真均以 100 HP 通关，主仓指标 0.98–0.993；42px 料口在这些场景没有持续桥拱堵塞。

主仓填充指标为上部颗粒数相对初始铺设数，允许局部重排后略大于 1，并非精确体积百分比。物理为小游戏近似；宝石沿竖直格轨道受约束下落。

## 浏览器与制作人

独立 Chrome 进程，实际鼠标点击/拖动及移动触摸输入，仅读取 `kingGame.snapshot()`；没有浏览器设胜、改 HP 或改盘面接口。截图和运行日志仅保存在忽略目录 `output/`。

实际浏览器回归已通过：桌面第一关首伤为 88 HP，之后真实拖动交换，记录到动态下落阶段且额外输入被拒绝；离刺时 28 HP，2 秒后仍 28 HP，最后获救。桌面第二关 5 次交换、100 HP 通关。390×844 移动触摸两关均通关，HP 100；暂停、无效交换、重排、重来及刷新通过，页面异常为 0。

移动布局曾发现按钮底部越界约 3px，已缩小画布预算并重测：390×844 按钮底 837.375px、格子 38.3px；360×800 按钮底 800px、格子 36px。

密集石料逐帧缩放原图带来长帧风险，渲染改为初始化时将原贴图缓存为 32px 位图；图源和物理不变。Chrome 的 360×800 / DPR2 模拟移动上下文在预热后 6.009 秒内记录 361 帧，p95 帧间隔 16.8ms，模拟推进 6.017 秒。新页面从开始按钮后立即采样 6.002 秒，同样 361 帧、p95 16.7ms、模拟推进 6.008 秒。此为本机桌面 Chrome，未覆盖实体手机硬件。

制作人独立盲玩与最终评分另见评审文档；本记录不替代其审批，也不把本地通过当作线上发布证明。
